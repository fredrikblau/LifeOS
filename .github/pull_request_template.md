## What this changes

<!-- One or two sentences. What behaviour is different after this PR? -->

## Why

<!-- The problem this solves. If it fixes a bug, what did the bug do to a real
user? "Reminders confirmed but never created never fire, and you find out by
missing the thing" beats "fixes reminder bug". -->

## How it was verified

<!-- Delete what doesn't apply. -->

- [ ] `./scripts/test.sh` passes (or `pytest tests/ -m "unit and not slow"`)
- [ ] Added a test that fails without this change
- [ ] Ran it against a real deployment

## Notes for the reviewer

<!-- Anything you're unsure about, a tradeoff you made, or a limitation you
chose to accept. Saying so is more useful than leaving it to be discovered. -->
